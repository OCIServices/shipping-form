== README

* Built and tested on Ruby 1.9.3-p392.

* Set a lot of environment varialbes in a file ".env" in the app root directory, using the sample

* Requires pdftk - may need to set the path in your ".env" file
